# Development environment configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_flow_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# JWT Configuration
app:
  jwt:
    secret: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970"
    issuer: payment-reconciliation-service
    audience: payment-reconciliation-api
    expiration: 86400

logging:
  level:
    "[com.example.paymentreconciliation]": DEBUG
    "[org.hibernate.SQL]": DEBUG
    "[org.hibernate.type.descriptor.sql.BasicBinder]": TRACE
  file:
    name: logs/dev.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%logger{36}] [%X{sessionToken}] [%X{X-Request-ID}] - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%logger{36}] [%X{sessionToken}] [%X{X-Request-ID}] - %msg%n"

file:
  upload:
    base-dir: /Users/rahulcharvekar/Documents/Repos/ManpowerManagement/PaymentReconciliation/payment-flow-service/uploads/dev/

management:
  endpoints:
    web:
      exposure:
        include: health,info,beans,env,metrics,loggers

# Auditing toggle
audit:
  enabled: true
  hashing-algorithm: SHA-256  # Optional: default is SHA-256

server:
  port: 8081
